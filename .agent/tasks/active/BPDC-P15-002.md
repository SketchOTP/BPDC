# BPDC-P15-002 — Activate Behavior Cooldowns

## Authority

- Directive: `BPDC-P15-002`
- Status: `IMPLEMENTED — PENDING ARCHITECT REVIEW`
- Repository: `\\atlas\ATLAS\100_ACTIVE\Projects\BPDC`
- Entry canonical SHA: `6f6b3dd51b76ad352664e0bb5b9af24547420e70`
- Local commit: pending
- Push: `NO — not authorized`
- Boundary: Phase 16 is not authorized.

## Scope

Activate the cooldown durations already declared in `BEHAVIOR_DEFINITIONS` through one bounded persistent `action -> availableAt` ledger. Cooldown affects eligibility only; it does not alter utility scores, action vocabulary, drives, preferences, or host behavior authority.

## Implemented boundary

- Added `behaviorCooldowns` as a bounded map keyed only by existing actions.
- Advanced snapshot schema from 6 to 7; schema-6 and earlier snapshots migrate with an empty ledger and no invented history.
- Natural exits record `exitSimulationTime + definition.cooldown` before selecting the next action.
- P14 midpoint abandonment records the old action's cooldown at the exact switch time; stable midpoint reconsideration does not.
- P7/P12 overlays do not interact with the ledger.
- Candidate diagnostics expose `baseEligible`, `cooldownEligible`, `cooldownUntil`, and `cooldownRemaining`; scores remain unchanged.
- Selection still consumes one noise draw per action, including cooldown-ineligible candidates. IDLE remains the fallback.
- Simulation time is the only cooldown clock; no wall-clock or adapter timer was introduced.
- Generated plugin core mirrors the source core, and the build script now anchors esbuild's working directory for cross-path deterministic output.

## Evidence

- `node --test`: 106/106 PASS.
- `node src/cli/experiments.js`: P15 A–K and the existing P4–P14 experiments PASS.
- Schema 6 → 7 migration, exact expiry, overlapping entries, P14 abandonment, stable midpoint, P7/P12 non-interference, save/reload, offline expiry, RNG discipline, SLEEP/AVOID, and IDLE fallback tests PASS.
- Two independent local staged builds: 80,238 bytes each; SHA-256 `F86F925C54F31DE5FE8DA88C7D19649F10645F439DE6422B8BAA9A9699BC2817`; tracked bundle is byte-identical.
- Manifest permissions are unchanged: `commands, events, pet:animate, pet:move, pet:reaction, pets:manage, pets:read, storage`.
- Syntax, CreatureCore host/privacy boundary scan, publication-safety scan, and `git diff --check`: PASS.
- Direct UNC esbuild remains blocked by known `spawn EPERM`; local staging is the validated workaround.
- Live desktop evidence was not required and is not claimed.

## Tooling limitation

jCodemunch-MCP was unavailable in this session. Local fallback inspection was used; repository indexing remains explicitly unverified.

## Required handoff

The implementation is complete for Architect review. Do not push and do not begin Phase 16.

