# BPDC-P16-001 — Local implementation handoff

## Status

`IMPLEMENTED — PENDING ARCHITECT REVIEW`; local product commit `42a77996143548e5bd022a82a2f823da5596fd3a`; not pushed.

## Scope

Added one bounded learned coarse daily activity routine by extending the existing habit state. Four local-time periods (`floor(localTime / 6)`) learn only `OBSERVE`, `WANDER`, `PLAY`, `FOLLOW_CURSOR`, and `SLEEP`. `IDLE`, `SEEK_ATTENTION`, and `AVOID` remain excluded.

Natural autonomous completion is the sole learning trigger. New commitments persist `routinePeriodAtStart`; midpoint-abandoned actions, P7/P12 overlays, and legacy active commitments without period metadata do not learn. Routine affinity uses saturating learning `0.04`, a 14-simulated-day half-life, and a maximum utility contribution of `0.12`. Existing P4 attention habit decay and scoring remain unchanged.

## Persistence and boundaries

- Snapshot schema: `7 → 8`.
- Habit schema: `1 → 2` with zeroed `activityByPeriod` for legacy state.
- No scheduler, planner, new action, drive, sensor, permission, negative habit, or Phase 17 work.
- CreatureCore remains host-independent and uses simulation time only.

## Validation

- `node --test`: **119/119 PASS**.
- `node src/cli/experiments.js`: **PASS**, including P16 routine experiments and P4–P15 regression experiments.
- Dedicated routine tests cover same-period learning, distributed history, natural completion, P14 abandonment, legacy migration, P4 isolation, decay, non-domination, eligibility, differentiation, save/reload, offline equivalence, and cooldown anti-lock.
- Syntax, manifest/permission comparison, CreatureCore boundary/privacy scan, secret/safety scan, and `git diff --check`: **PASS**.
- Two independent staged builds and tracked generated bundle: **84,153 bytes**, SHA-256 `E61BE612F6EFE8B77CEE6B15A1CB43F1435981B180366BCAD31E6675E7E3D2BF`; all byte/hash-equal.
- Direct UNC esbuild remains blocked by known `spawn EPERM`; local staging is the validated build route.
- jCodemunch-MCP was unavailable in this session; repository indexing remains unverified.

## Evidence boundary

`E3_TARGET_TESTED` pending Architect acceptance. Live Windows/Electron routine behavior is `UNKNOWN / NOT CLAIMED` and was not fabricated. Phase 17 remains not authorized.
