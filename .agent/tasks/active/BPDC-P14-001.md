# BPDC-P14-001 — Midpoint Reconsideration

## Authority

- Directive: `BPDC-P14-001`
- Status: `IMPLEMENTED_PENDING_ARCHITECT_ACCEPTANCE`
- Repository: `\\atlas\ATLAS\100_ACTIVE\Projects\BPDC`
- Entry canonical SHA: `9c656f017755f53e34c009c5751b192cce55d221`
- Local product commit: `e630dc6` (`feat: add midpoint behavior reconsideration`)
- Push: `NO — publication awaits a subsequent Architect decision`
- Boundary: Phase 15 is not authorized.

## Scope

Add exactly one deterministic midpoint reconsideration to existing interruptible behavior commitments. The checkpoint is derived as `startedAt + duration * 0.5`, performs one fresh deterministic utility comparison, and may switch only for current ineligibility or a challenger at least `0.15` above the current score. No new action, persistent state, schema, permission, cursor data, planner, or behavior authority is introduced.

## Implemented boundary

- `SLEEP` and `AVOID` remain non-interruptible; existing interruptible behaviors, including `FOLLOW_CURSOR`, receive at most one midpoint checkpoint.
- The gate consumes no RNG. A switch commits through the existing behavior authority and samples only the ordinary replacement duration.
- Large and partitioned advances segment at the exact midpoint; offline reconciliation suppresses historical intents and host commands.
- Reload does not invent a retroactive checkpoint and no checkpoint field is persisted.
- P7/P12 continue using the single generation-guarded transient restoration slot; new autonomous behavior supersedes stale restoration safely.
- OpenPets adapter behavior and permissions are unchanged.

## Evidence

- `node --test`: 94/94 PASS.
- Combined P4–P14 experiments: PASS, including stable midpoint, switch, eligibility, hysteresis, non-interruptible, partition equivalence, persistence, offline suppression, and transient arbitration.
- Two independent clean staged builds and tracked generated bundle: 77,281 bytes; SHA-256 `F0671BCF39B9FF2DCF4BEF61DFB195A5B5F5CBACC8B42F66F9BDCEFEF1A43A38`; byte-identical.
- Syntax, manifest/unchanged permissions, boundary/privacy, publication-safety, and `git diff --check`: PASS.
- Direct UNC esbuild remains blocked by known `spawn EPERM`; local staging is the validated workaround.
- Live Windows/Electron midpoint observation: `UNKNOWN / NOT CLAIMED`; not required for this handoff.

## Tooling limitation

jCodemunch-MCP was unavailable in this session. Local fallback exploration was used; the repository indexing requirement remains explicitly unverified.

## Required handoff

Return `CODEX RESULT — BPDC-P14-001`. Commit locally, do not push, and do not begin Phase 15.
